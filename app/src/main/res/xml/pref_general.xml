<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:title="@string/pref_games_label"
        android:key="@string/pref_game_key"
        android:defaultValue="@string/pref_game_default"
        android:entryValues="@array/pref_list_game_ids_values"
        android:entries="@array/pref_list_game_ids" />

</PreferenceScreen>
